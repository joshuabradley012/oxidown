{
  "name": "oxidown-workspace",
  "private": true,
  "scripts": {
    "build:wasm": "wasm-pack build crates/oxidown-wasm --target web --out-dir pkg",
    "dev": "pnpm --filter web-demo dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
